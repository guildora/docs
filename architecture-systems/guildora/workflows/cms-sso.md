# Workflow: CMS SSO

> **Deprecated:** CMS SSO has been removed along with the Payload CMS app (`apps/cms`). The hub no longer has a `/api/cms/session-url` endpoint or CMS access policy. Landing page content is managed directly in Hub via the `/settings/landing` editor and DB-backed tables.
