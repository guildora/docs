# Localization

The marketplace app is **English-only**. There is no i18n module or language switching.

All user-facing text is written directly in Vue templates as plain English strings.

Landing page content is managed in `content/landing.md` (YAML frontmatter) and loaded via `@nuxt/content`.
